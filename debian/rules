#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@  --with python2

override_dh_auto_test:
	nosetests


get-orig-source:
	uscan --verbose --rename --destdir=../build-area
